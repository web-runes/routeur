{
  "name": "spike-vue",
  "private": true,
  "type": "module",
  "description": "Throwaway spike: Vue SFC generic props (B) + SSR prop mutation (C)",
  "scripts": {
    "typecheck": "vue-tsc --noEmit -p tsconfig.json",
    "ssr": "node --experimental-strip-types spike-c-ssr.ts"
  },
  "devDependencies": {
    "vue": "^3.5.13",
    "vue-tsc": "^2.2.0",
    "typescript": "^5.7.3"
  }
}
